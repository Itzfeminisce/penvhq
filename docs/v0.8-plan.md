# v0.8 plan — ambient delivery: the schema is the pin list

> **Status: active.** Decided 2026-07-19, in deliberation over a real wound:
> penv-cloud — penv's own hosted product, dogfooding `@env` strictly — had to
> hand-write `bridge-values.ts`, a file that maps `@env` values onto the exact
> `process.env` keys the WorkOS SDK reads, plus a hand-kept list of ambient keys
> to delete so nothing configures the SDK behind `@env`'s back. That is the
> local↔consumer translation seam penv exists to delete, rebuilt by hand inside
> penv's own product. This milestone deletes it. The [RFC](./RFC.md) owns *why*
> (see "The ambient surface is a projection, and the schema is the pin list");
> this file owns *how*, and in what order.

**One framing to carry through.** Three kinds of code read configuration: your
own code (`import { env } from "@env"` — solved), third-party SDKs that read
`process.env.THEIR_EXACT_NAME` at module load (WorkOS, Prisma, NextAuth — the
wound), and client bundles the framework inlines at build time (`NEXT_PUBLIC_*`,
`VITE_*` — half-solved via the `next.config.ts` import). v0.8 makes all three
read the same truth with zero per-SDK code, using **no new vocabulary**: the
concept is v0.7's *projection* delivered into the local process instead of into
GitHub, and every declaration involved already exists — the schema, `override`
(né `names`), and an import penv already ships.

---

## The settled surface

1. **`load(schema, { inject: true })` — the blessed ambient surface.** *(Shipped;
   see PR "the blessed ambient inject surface".)* Called from `.penv/env.ts`,
   because validating and being exclusive both need the *schema*, which only
   `env.ts` holds — the bare `import "@penvhq/penv/config"` never sees it and
   stays the schemaless dotenv-compat path. Three properties:
   - **Validate-first.** `load` parses the values against the schema and throws a
     parameter-named error before injecting, so an SDK never sees a
     half-configured surface — a missing required parameter fails at boot with
     penv's error, not the SDK's mystery three requests in.
   - It writes each parameter under its generated variable (`override` bending
     names exactly as `generate` and `push` already do — one transform, three
     consumers), as the **raw** string (`process.env` is strings; the SDK
     re-parses).
   - **Exclusivity — the schema is the pin list.** Every parameter the schema
     declares is penv's ambiently: written to `process.env` when it has a value —
     a tree value or a schema `.default()` — and **deleted** when it has none.
     Declare `workos/api-hostname` as `.optional()` and an ambient
     `WORKOS_API_HOSTNAME` can never steer the SDK behind `@env`'s back; leave it
     undeclared and penv does not touch it. The declaration the developer already
     makes is the whole contract — no second list, which is what kills the
     `PINNED_OPTIONAL_KEYS` class of hand-kept files. Off by default — a consumer
     who never asked for `process.env` writes gets none.

2. **The ordering race is solved at the framework's own pre-app seam, and
   `detect` owns which seam that is.** *(Next up.)* The injection only helps if
   it runs before the SDK module reads `process.env`, and module-graph order is
   not something a developer controls. So `penv init` scaffolds an `import "@env"`
   (which runs `env.ts`, hence the injection) into the one place each framework
   *guarantees* runs first — detection as an input to `init`, never a stored
   identity (invariant 22), exactly as `schemaFile` and `publicPrefixes` already
   work:

   | Framework | Server runtime seam | Build-time (client vars) seam |
   |---|---|---|
   | Next.js | `instrumentation.ts` | `next.config.ts` |
   | Nuxt / Nitro | a Nitro plugin (`server/plugins/penv.ts`) | `nuxt.config.ts` |
   | TanStack Start | its server entry | `vite.config.ts` |
   | Vite SPA (no server) | — (does not exist) | `vite.config.ts` |
   | SvelteKit | `hooks.server.ts` | `vite.config.ts` |
   | Plain Node / Express / Fastify | `node --import` in the start script, or line 1 of the entry | — |
   | Bun | `preload` in `bunfig.toml` | — |

   Two honesty rules ride with the table. A client-only app has no runtime half,
   so `init` scaffolds only the build half and says so — which halves *exist* is
   part of detection. An unrecognised framework is asked, never guessed: `init`
   prints the import and the `node --import` form and lets the developer place
   it knowingly. And the table above is *planning* data — each scaffold is
   implemented against the framework's current documentation at build time, not
   from this file's memory.

3. **`names` becomes `override`, with typed keys.** The block maps a parameter
   to the exact variable a consumer expects — `override` is what it does. Keys
   are typed by the same declaration-merging mechanism the providers use: the
   scaffolded `.penv/env.ts` registers the schema's inferred shape on core's
   `PenvSchemaShape` (a *type-only* `declare module`, erased at runtime, so no
   config↔schema cycle), and `override`'s key type narrows to the parameter ids
   the schema declares — camelCase kebab-cased, mirroring the runtime transform.

   ```ts
   // .penv/env.ts — penv init writes this block
   declare module "@penvhq/core" {
     interface PenvSchemaShape {
       readonly shape: z.infer<typeof schema>;
     }
   }
   ```

   ```ts
   // penv.config.ts — override keys now autocomplete; a typo is a compile error
   override: { "workos/redirect-uri": "NEXT_PUBLIC_WORKOS_REDIRECT_URI" },
   ```

   No registration means the keys stay the open `string` type — typing is a
   reward for the one scaffolded line, never a requirement. (Shipped early with
   the rename PR rather than held for v0.8; the exported `OverrideKeysOf<T>`
   transform lets a project opt in by hand too.)

4. **`doctor` `ambient-shadow` — dropped (2026-07-20).** The idea was a check that
   flagged an ambient `process.env` key colliding with a schema-declared
   variable. It does not survive contact with where `doctor` runs: at doctor-time
   `process.env` is the developer's *shell*, not the app's injected runtime, so a
   declared variable being present there is the normal case (it is set in most
   shells and every CI), not evidence of a shadow. The check could not tell the
   two apart, and a warning that fires on the normal case is one everyone learns
   to skim past — the exact failure the [unknown-verdict decision](./RFC.md#doctor-answers-i-cannot-tell-as-its-own-verdict)
   guards against. Dropped rather than shipped vague. If a precise signal is found
   later (comparing against a materialised injection, say), it can return with a
   real spec.

## What the developer does

```bash
npx penv init        # scaffolds the seam files for the detected framework
```

Then only ever: declare parameters in the schema, `penv set` values, and add an
`override` entry when an SDK demands a name penv would not generate. Every way
of reading configuration — `@env`, `process.env`, the client bundle — sees the
same validated truth.

## Build order

1. **PR: `names` → `override` + typed keys.** ✅ *Shipped in 0.5.0.* Breaking;
   `CONFIG_NAMES_RENAMED` migration error for a config still saying `names`.
2. **PR: the blessed inject surface.** ✅ *Shipped.* `load(schema, { inject: true })`
   — validate-first, schema-exclusive, off by default, driven from `env.ts`
   (not the schemaless bare import). `ambient-shadow` and the seams split out to
   their own PRs below.
3. **PR: the seams.** *Next.* `init`/`detect` scaffold `import "@env"` at each
   framework's pre-app hook, halves only where they exist, ask-don't-guess
   fallback; Documentation.md's compat caveat replaced by the blessed story.
4. **PR: `doctor` `ambient-shadow`.** *Next.*

**Gate.** penv-cloud deletes `bridge-values.ts` and `env-bridge.ts`, moves the
WorkOS surface (required and optional keys alike) into its schema plus one
`override` entry, runs the scaffolded seams — and WorkOS authenticates with no
penv-aware code anywhere in the app. The check that proves exclusivity: export a
stray `WORKOS_API_HOSTNAME`, boot, and confirm the SDK never sees it.

## Deliberately deferred

- **A lazy `process.env` proxy** (ordering independence with no seam file at
  all). Real magic, real platform risk — edge runtimes and env-snapshotting
  hosts break it — so it stays a future possibility until the eager injection
  proves the semantics.
- **Selective exposure** (mirroring less than the whole schema). The dotenv
  world has always had every value ambient in-process; if a team asks for a
  narrower surface, that is a deliberation, not a hedge shipped now.

## What this plan deliberately does not do

- **It does not touch the runtime's shape.** `load()` stays synchronous and
  disk-backed; the mirror is a delivery step downstream of resolution, on the
  same side of the design as `generate`.
- **It does not add a `framework` key to config** — invariant 22 stands.
  Detection happens at `init`, what lands is the scaffolded files.
- **It does not make penv ambient by default.** No import, no mirror: a library
  consumer who never asked for `process.env` writes gets none.
